---
name: llm-wiki-lint
description: wikiの健全性チェック。ユーザーが「lintして」「wikiをlintして」と言った時に使用する。
---

# Lint

wikiの健全性チェック。ユーザーの明示的な依頼で発動する。
機械チェックはスクリプト（決定的）、意味チェックは読解（LLMの判断）で行う。

## 手順

1. 機械チェック: `bash scripts/lint.sh` を実行する
   - 孤立ページ（他のwikiページからの入リンクがない）
   - 壊れた `[[wikilink]]` — 参照数で分類:
     - **1ページのみ参照 = typoまたは意図的赤リンク**。typoなら修正、意図的なら放置（ページを作らない）
     - **2ページ以上で参照 = 概念ハブの種**。エラーではなく、ページ作成の候補として人間に提案
   - index.md に載っていないページ
2. 意味チェック（手動）:
   - ページ間の矛盾（同一トピックで衝突する主張）
   - 古い主張（新しいソースに取って代わられたもの）
   - ページに言及されているのに概念ページが存在しない概念
   - 明らかに関連するページ間のクロスリファレンス不足
3. 新しい調査課題と探すべきソースを提案する
4. 重大度別に報告し、機械的な問題の自動修正を申し出る

## 注意

- 赤リンクは許可された設計（情報の不在の表現）。壊れたリンクに見えても、意図的なら放置する
- lint結果の解釈はLLMが行う。スクリプトの出力をそのまま報告するだけでなく、優先度をつけて伝える